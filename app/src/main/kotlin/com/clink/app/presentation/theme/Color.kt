package com.clink.app.presentation.theme

import androidx.compose.ui.graphics.Color

// ==========================================
// Brand Core Palette
// ==========================================
val ClinkPink = Color(0xFFE85D75)         // Vibrant playful brand primary
val ClinkPinkLight = Color(0xFFFF8EA0)    // Soft pastel pink for highlights & piggy body
val ClinkPinkDark = Color(0xFFC73E56)     // Deep rose for containers and pressed states

val ClinkNavy = Color(0xFF172033)         // Modern trustworthy fintech navy
val ClinkNavyLight = Color(0xFF263552)    // Slate accent navy
val ClinkNavyDark = Color(0xFF0D131F)     // Rich dark mode background (warm, not pure black)
val ClinkNavySurface = Color(0xFF161F30)  // Elevated dark mode surface

val ClinkTeal = Color(0xFF35B8A6)         // Fresh mint teal for positive savings / growth
val ClinkTealLight = Color(0xFF6DE8D5)    // Soft teal tint
val ClinkTealDark = Color(0xFF1E8274)     // Deep teal for high contrast text

// ==========================================
// Mascot & Coin Illustration Palette
// ==========================================
val CoinGold = Color(0xFFFFB703)          // Warm golden coin
val CoinGoldLight = Color(0xFFFFD166)     // Coin gleam / highlight
val PiggyBlush = Color(0xFFFFAAA6)        // Rosy cheeks
val PiggyEarInside = Color(0xFFFFB5C2)    // Ear inner shadow

// ==========================================
// Semantic Status Palette
// ==========================================
val SuccessGreen = Color(0xFF2EC4B6)
val SuccessGreenContainerLight = Color(0xFFE4F8F5)
val SuccessGreenContainerDark = Color(0xFF133B36)

val WarningOrange = Color(0xFFFF9F1C)
val WarningOrangeContainer = Color(0xFFFFF3E0)

val ErrorRed = Color(0xFFE63946)
val ErrorRedContainerLight = Color(0xFFFFECEE)
val ErrorRedContainerDark = Color(0xFF3D1519)

// ==========================================
// Light Theme Neutrals
// ==========================================
val BackgroundLight = Color(0xFFF7F8FA)
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceVariantLight = Color(0xFFF0F3F7)
val OutlineLight = Color(0xFFD9DFE8)
val OutlineVariantLight = Color(0xFFE8ECF2)
val OnSurfaceLight = Color(0xFF172033)
val OnSurfaceVariantLight = Color(0xFF6C768A)

// ==========================================
// Dark Theme Neutrals (Warm, low-strain slate)
// ==========================================
val BackgroundDark = Color(0xFF0D131F)
val SurfaceDark = Color(0xFF161F30)
val SurfaceVariantDark = Color(0xFF1E2A40)
val OutlineDark = Color(0xFF2C3C59)
val OutlineVariantDark = Color(0xFF233047)
val OnSurfaceDark = Color(0xFFECEFF4)
val OnSurfaceVariantDark = Color(0xFF9CA9BD)
